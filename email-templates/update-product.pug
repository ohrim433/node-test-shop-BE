extends template
block content
    div.w-800.message
        p Hello, #{userName}!
        | You updated a new product #{productTitle}
        br
        | Actual data is:
        ul
            li title: #{productTitle}
            li type: #{productType}
            li price: #{productPrice}
